name: sandbox tests

on: 
  workflow_call:
  push:

jobs:
  
  build:

    runs-on: ubuntu-latest

    steps:
      
    - uses: actions/checkout@v2

    - name: run bash
      if: always()
      run: |
        cd .github/workflows
        ./sandbox.sh

