name: EDAM-VALIDATION 

on: [push, workflow_dispatch]

jobs:
  
  edamci:
    uses: edamontology/edamontology/.github/workflows/web_app_edamci.yml@workflow_validation_web_app

  report:
    uses: edamontology/edamontology/.github/workflows/robot_report.yml@workflow_validation_web_app
